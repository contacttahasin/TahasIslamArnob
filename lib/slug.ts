/** Lowercase, hyphenated, alphanumeric-only — matches projectSchema's slug regex. */
export function slugify(input: string): string {
  return input
    .trim()
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/^-+|-+$/g, "");
}
